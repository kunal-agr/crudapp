services:
  crudapp:
    build:
      context: .
      dockerfile: Dockerfile
    image: crudapp:latest
    container_name: crudapp-tomcat
    ports:
      - "8080:8080"
    env_file:
      - ./src/main/resources/application.env
    restart: always  # This keeps the container alive even if a transient error occurs